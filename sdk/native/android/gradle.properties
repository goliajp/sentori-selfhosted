# AndroidX, because `androidx.core` is a dependency and the build
# refuses to resolve it otherwise.
android.useAndroidX=true

# The Gradle daemon's default heap is not enough for AGP 8 plus
# Robolectric's android-all download on a cold CI runner.
org.gradle.jvmargs=-Xmx3g -XX:MaxMetaspaceSize=1g
